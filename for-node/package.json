{
  "name": "package-name",
  "version": "0.0.0",
  "private": true,
  "description": "Package description",
  "author": "Author Name",
  "license": "UNLICENSED",
  "homepage": "https://example.com/",
  "repository": {
    "type": "git",
    "url": "https://example.com/repository"
  },
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "fmt": "echo 'Skip task fmt'",
    "lint": "tsc --noEmit && eslint --ext .ts,.tsx,.js src",
    "build": "npm run build:setup && npm run build:bundle && npm run build:teardown",
    "build:setup": "rm -rf dist",
    "build:bundle": "tsc && esbuild dist/main.js --outfile=public/main.bundle.js --bundle --minify",
    "build:teardown": "echo 'Skip task build:teardown'",
    "test": "npm run test:setup && npm run test:unit && npm run test:e2e && npm run test:teardown",
    "test:setup": "echo 'Skip task test:setup'",
    "test:unit": "NODE_OPTIONS='--experimental-vm-modules' jest --ci --runInBand --passWithNoTests",
    "test:e2e": "echo 'Skip task test:e2e'",
    "test:teardown": "echo 'Skip task test:teardown'",
    "serve": "echo 'Skip task serve'"
  },
  "devDependencies": {
    "@jest/globals": "*",
    "@testing-library/react": "*",
    "@types/react": "*",
    "@types/react-dom": "*",
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "esbuild": "*",
    "eslint": "*",
    "eslint-plugin-react": "*",
    "eslint-plugin-react-hooks": "*",
    "jest": "*",
    "jest-environment-jsdom": "*",
    "jest-junit": "*",
    "jest-mock": "*",
    "prettier": "*",
    "react": "*",
    "react-dom": "*",
    "react-router-dom": "*",
    "recoil": "*",
    "ts-jest": "*",
    "typescript": "*"
  }
}
