{
  "name": "package-name",
  "version": "0.0.0",
  "private": true,
  "description": "Package description",
  "author": "Author Name",
  "license": "UNLICENSED",
  "homepage": "https://example.com/",
  "repository": {
    "type": "git",
    "url": "https://example.com/repository"
  },
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "fmt": "echo 'Skip task fmt'",
    "lint": "tsc --noEmit && eslint --ext '.ts,.tsx,.js' src",
    "build": "npm run fmt && npm run lint && npm run build:setup && npm run build:bundle && npm run build:teardown",
    "build:setup": "rm -rf dist",
    "build:bundle": "tsc && esbuild dist/main.js --outfile=public/main.bundle.js --bundle --minify",
    "build:teardown": "echo 'Skip task build:teardown'",
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest --ci --runInBand --passWithNoTests"
  },
  "devDependencies": {
    "@testing-library/react": "*",
    "@types/jest": "*",
    "@types/react": "*",
    "@types/react-dom": "*",
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "esbuild": "*",
    "eslint": "*",
    "eslint-plugin-react": "*",
    "eslint-plugin-react-hooks": "*",
    "jest": "*",
    "jest-environment-jsdom": "*",
    "jest-junit": "*",
    "jest-mock": "*",
    "prettier": "*",
    "react": "*",
    "react-dom": "*",
    "react-router-dom": "*",
    "recoil": "*",
    "ts-jest": "*",
    "typescript": "*"
  }
}
